@namespace GumihoUI

<div class="row @(FitColumns ? "fit" : string.Empty)">
    @ChildContent
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public bool FitColumns { get; set; }
}
